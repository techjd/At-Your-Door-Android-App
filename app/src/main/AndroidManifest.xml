<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.atyourdoorteam.atyourdoor">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".UI.Activities.SubCategoryProductActivity" />
        <activity android:name=".UI.Activities.OrdersFailed" />
        <activity android:name=".UI.Activities.OrderPlaced" />
        <activity android:name=".UI.Activities.OurMottoActivity" />
        <activity android:name=".UI.Activities.PrivacyPolicyActivity" />
        <activity android:name=".UI.Activities.OrdersActivity" />
        <activity android:name=".UI.Activities.SearchActivity" />
        <activity android:name=".UI.Activities.ChooseModeOfPayment" />
        <activity android:name=".UI.Activities.SelectStoreActivity" />
        <activity android:name=".UI.Activities.CheckOutActivity" />
        <activity android:name=".UI.Activities.AddressActivity" />
        <activity android:name=".UI.Activities.AllShopsActivity" />
        <activity android:name=".UI.Activities.ProductsActivity" />
        <activity android:name=".UI.Activities.ShopCategoriesActivity" />
        <activity android:name=".UI.Activities.AllCategoriesActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".UI.Activities.MainActivity" />
        <activity
            android:name=".UI.Activities.ForgotPasswordActivity"
            android:noHistory="true" />
        <activity
            android:name=".UI.Activities.RegistrationActivity"
            android:noHistory="true" />
        <activity
            android:name=".UI.Activities.LoginActivity"
            android:noHistory="true" />
        <activity
            android:name=".UI.Activities.ChooseLogInORSignUpActivity"
            android:noHistory="true" />
        <activity android:name=".UI.Activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>